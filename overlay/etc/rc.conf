# /etc/rc.conf — ShedOS overrides on top of Alpine defaults.

rc_logger="YES"
rc_log_path="/var/log/rc.log"
rc_parallel="YES"
rc_interactive="NO"

# Don't drop into a single-user shell on boot failure — the brain or sshd
# can recover, and a hung getty wouldn't be reachable anyway.
rc_shell="NO"
